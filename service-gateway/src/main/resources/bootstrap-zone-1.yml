eureka:
  instance:
    metadataMap:
      zone: zone-1
  client:
    preferSameZoneEureka: true
    service-url:
      defaultZone: @eureka.client.service-url.defaultZone@

spring:
  cloud:
    loadbalancer:
      zone: zone-1