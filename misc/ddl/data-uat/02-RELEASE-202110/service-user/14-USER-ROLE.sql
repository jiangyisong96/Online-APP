INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat39%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat40%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat41%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat43%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat44%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat421%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat422%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat423%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat424%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat425%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat427%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat428%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat429%'
  AND t2.name IN (
                  'ROLE_CANDIDATE','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'candidateUat426%'
  AND t2.name IN (
    'ROLE_CANDIDATE'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'coordinatorAcs7%'
  AND t2.name IN (
                  'ROLE_COORDINATOR','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'coordinatorAcs8%'
  AND t2.name IN (
                  'ROLE_COORDINATOR','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'coordinatorAcs9%'
  AND t2.name IN (
                  'ROLE_COORDINATOR','ROLE_CP_USER'
    );

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'coordinatorAcs10%'
  AND t2.name IN ('ROLE_COORDINATOR','ROLE_CP_USER');

INSERT INTO SERVICE_USER.user_role (user_id, role_id)
SELECT t1.id,
       t2.id
FROM SERVICE_USER.users t1,
     SERVICE_USER.roles t2,
     SERVICE_USER.user_credentials t3
WHERE t1.user_credentials_id = t3.id
  AND t3.username LIKE 'coordinatorAcs11%'
  AND t2.name IN (
                  'ROLE_COORDINATOR','ROLE_CP_USER'
    );